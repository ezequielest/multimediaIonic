import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CambioPage } from './cambio';

@NgModule({
  declarations: [
    CambioPage,
  ],
  imports: [
    IonicPageModule.forChild(CambioPage),
  ],
})
export class CambioPageModule {}
